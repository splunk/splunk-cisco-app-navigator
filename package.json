{
  "name": "cisco-control-center-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "setup": "yarn && lerna run build",
    "start": "cd packages/cisco-control-center-app && yarn start",
    "lint": "lerna run lint",
    "clean": "lerna run clean --parallel"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
